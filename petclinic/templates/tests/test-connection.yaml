apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "petclinic.fullname" . }}-test-connection"
  labels:
    {{ include "petclinic.labels" . | nindent 4 }}
spec:
  containers:
    - name: curl
      image: curlimages/curl
      command: ['curl']
      args: ['{{ include "petclinic.fullname" . }}:8080']
